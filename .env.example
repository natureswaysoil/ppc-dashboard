# ============================================================================
# Amazon PPC Dashboard - Environment Variables
# ============================================================================
# Copy this file to .env.local for local development
# For production (Vercel/etc), set these in your platform's environment config
# NEVER commit .env.local to version control!

# ============================================================================
# DASHBOARD API CONFIGURATION
# ============================================================================
# API key used by optimizer to authenticate with dashboard
# Must match the DASHBOARD_API_KEY secret in Cloud Function
DASHBOARD_API_KEY=your_dashboard_api_key_here

# ============================================================================
# GOOGLE CLOUD CREDENTIALS (REQUIRED FOR BIGQUERY)
# ============================================================================
# Choose ONE method to provide service account credentials:

# METHOD 1: Raw JSON (Recommended - works everywhere)
# Copy entire service account key JSON file contents (all on one line):
GCP_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"...","client_email":"..."}

# METHOD 2: Base64-Encoded JSON (Alternative if raw JSON has issues)
# Create: cat service-account.json | base64 | tr -d '\n'
# Then set GCP_SERVICE_ACCOUNT_KEY to the base64 output
# The dashboard automatically detects and decodes base64 credentials

# METHOD 3: File Path (Local development only - won't work on Vercel)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# METHOD 4: Component Credentials (Advanced/Alternative)
# GCP_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
# GCP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ============================================================================
# GOOGLE CLOUD PROJECT CONFIGURATION
# ============================================================================
# Your Google Cloud project ID
GCP_PROJECT=amazon-ppc-474902
GOOGLE_CLOUD_PROJECT=amazon-ppc-474902

# ============================================================================
# BIGQUERY CONFIGURATION
# ============================================================================
# BigQuery dataset where optimizer writes data
BQ_DATASET_ID=amazon_ppc

# BigQuery dataset location/region
BQ_LOCATION=us-east4

# ============================================================================
# OPTIONAL: DATABASE (if using external database)
# ============================================================================
# DATABASE_URL=your_database_url_here

# ============================================================================
# OPTIONAL: AUTHENTICATION (if using NextAuth)
# ============================================================================
# NEXTAUTH_SECRET=your_nextauth_secret_here
# NEXTAUTH_URL=https://amazonppcdashboard.vercel.app

# ============================================================================
# OPTIONAL: AMAZON API (if dashboard needs direct API access)
# ============================================================================
# AMAZON_CLIENT_ID=your_client_id_here
# AMAZON_CLIENT_SECRET=your_client_secret_here
# AMAZON_REFRESH_TOKEN=your_refresh_token_here
# AMAZON_PROFILE_ID=your_profile_id_here

# ============================================================================
# VERIFICATION ENDPOINTS
# ============================================================================
# After setting variables, test configuration:
#   /api/credentials-debug - Detailed credential diagnostics
#   /api/config-check      - Overall configuration check  
#   /api/bigquery-data     - Test BigQuery connection
#
# See README_DASHBOARD_SETUP.md for complete setup guide
